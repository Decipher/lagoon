# Default values for lagoon-logs-concentrator.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: amazeeio/logs-concentrator
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart version.
  tag: logs-concentrator

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 24224

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths: []
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  requests:
    cpu: 250m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 4
  targetCPUUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

# Elasticsearch output config. Defaults to 9300, http.
#elasticsearchHostPort: "443"
#elasticsearchScheme: https

# The values below must be supplied during installation as they have no sane
# defaults. Certificate should be provided in PEM format.
#elasticsearchHost: ""
#elasticsearchAdminPassword: ""
#tls:
#  caCert: ""
#  serverCertChain: ""
#  serverKey: ""
